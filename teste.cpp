
v4l2_control c;

c.id = V4L2_CID_EXPOSURE_AUTO; //ID for setting exposure to automatic adjustments. Type enum v4l2_exposure_auto_type
c.id = V4L2_CID_EXPOSURE; //ID for exposure adjust. Type integer
c.id = V4L2_CID_EXPOSURE_ABSOLUTE; //ID to determine the exposure time of the camera sensor. Type integer
c.id = V4L2_CID_PAN_RELATIVE; // ID to turn the camera horizontally by the specified amount. Type integer
c.id = V4L2_CID_TILT_RELATIVE; // ID to turn the camera vertically by the specified amount. Type integer
c.id = V4L2_CID_FOCUS_RELATIVE; // ID to move the focal point of the camera by the specified amount. Type integer
c.id = V4L2_CID_FOCUS_AUTO; // ID to enable continuous automatic focus adjustments. Type boolean
c.id = V4L2_CID_ZOOM_RELATIVE; //ID wich specifies the objective lens focal length relatively to the current value. Type integer
c.id = V4L2_CID_BRIGHTNESS; // ID for brightness adjustment. Type integer
c.id = V4L2_CID_CONTRAST; // ID for Picture contrast or luma gain. Type integer
c.id = V4L2_CID_SATURATION; // ID for picture color saturation or chroma gain. Type integer
c.id = V4L2_CID_HUE; // ID for hue or color balance. Type integer
c.id = V4L2_CID_AUTO_WHITE_BALANCE; // ID for automatic white balance. Type boolean
c.id = V4L2_CID_GAMMA; // ĨD for gamma adjust. Type integer
c.id = V4L2_CID_AUTOGAIN; // 
c.id = 
c.id = 
c.id = 
c.id = 
c.id = 

c.value = V4L2_EXPOSURE_MANUAL;



